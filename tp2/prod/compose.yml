services:
  mariadb:
    extends:
      file: ../compose.yml
      service: mariadb

  phpmyadmin:
    image: phpmyadmin:5.2.2
    ports:
      - 8080:80
    environment:
      PMA_ARBITRARY: 1
    depends_on:
      - mariadb

volumes:
  database:
